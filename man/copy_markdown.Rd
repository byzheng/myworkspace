% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copy_markdown.R
\name{copy_markdown}
\alias{copy_markdown}
\title{Copy Markdown Files to a Destination Directory}
\usage{
copy_markdown(
  src = ".",
  dst,
  overwrite = TRUE,
  keep_md = FALSE,
  exclude = c("_site", ".quarto", ".git", ".targets", "_freeze", ".Rproj.user",
    ".vscode")
)
}
\arguments{
\item{src}{Character scalar. Source directory to scan for markdown files.
Defaults to the current working directory.}

\item{dst}{Character scalar. Destination directory where markdown files will
be copied.}

\item{overwrite}{Logical scalar. Whether to overwrite existing files in the
destination when the source file is newer. Defaults to TRUE.}

\item{keep_md}{Logical scalar. Whether to keep the original markdown files that
have the `.html.md` extension. Defaults to FALSE, which deletes these files after copying.}

\item{exclude}{Character vector. Directory names to skip when scanning for
markdown files. Defaults to common build and editor directories.}
}
\value{
Invisible character vector of relative paths copied to the
  destination directory.
}
\description{
Recursively copies markdown files from a source directory to a destination
directory while skipping excluded directories such as build outputs or git
metadata. Existing files are only overwritten when requested.
}
\examples{
\dontrun{
copy_markdown(src = "./docs", dst = "./site-docs")
}
}
