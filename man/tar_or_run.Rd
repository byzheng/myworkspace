% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar_or_run.R
\name{tar_or_run}
\alias{tar_or_run}
\title{Try to Read a targets Target, or Run a Fallback Expression}
\usage{
tar_or_run(target, expr, quiet = FALSE, ...)
}
\arguments{
\item{target}{Unquoted name of the target to read (not a string).}

\item{expr}{Expression to evaluate if target read fails. Will be evaluated
lazily only if needed.}

\item{quiet}{Logical. If `FALSE` (default), print messages explaining why
the target read failed and that the fallback is running. If `TRUE`,
suppress these messages.}

\item{...}{Additional arguments passed to `targets::tar_read()`, such as `branches` or `store`. This allows for more flexible target reading, such as reading specific branches of a dynamic target or from a non-default store.}
}
\value{
The value of the read target if successful, otherwise the result of
  evaluating `expr`.
}
\description{
Attempts to read a computed target from a targets pipeline using
`targets::tar_read()`. If the target cannot be read (e.g., it hasn't been
computed yet), runs a fallback expression instead. Useful in interactive
workflows where a target may not always be available.
}
\examples{
\dontrun{
# Try to read a computed target, fall back to a simple value
tar_or_run(my_data, data.frame(x = 1:5))

# Suppress messages when using interactively
tar_or_run(results, compute_results(), quiet = TRUE)
}
}
